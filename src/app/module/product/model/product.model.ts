export class Product {
    constructor(public productNo: number, public name: string, public price: number, public size: string, public details: string[], public fabricAndCare: string[], public photos: string[]) {};
}